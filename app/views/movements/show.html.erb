<%= @movement.name %>

<%= button_to t('.back'), movements_path, method: :get %>
<%= button_to t('.edit'), edit_movement_path(@movement), method: :get %>
<%= button_to t('.destroy'), movement_path(@movement), method: :delete, data: { confirm: t(:confirm_message) } %>
